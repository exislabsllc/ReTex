export {
  escapeHtml,
  escapeAttribute,
  sanitizeUrl,
  isSafeColor,
  isSafeDimension,
  sanitizeStyleValue,
  CSS_NAMED_COLORS,
} from "./sanitize.js";
export type { UrlSanitizeResult } from "./sanitize.js";
